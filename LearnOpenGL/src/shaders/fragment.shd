#version 460 core

out vec4 fragColor;

in vec3 vertexColor;
in vec2 texCoord;

uniform sampler2D tex1;
uniform sampler2D tex2;

void main()
{
    fragColor = texture(tex2, texCoord) * vec4(vertexColor.zyx, 1.0);
}