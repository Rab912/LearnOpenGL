#version 460 core

out vec4 fragColor;

in vec3 vertexColor;
in vec2 texCoord;

uniform sampler2D tex1;
uniform sampler2D tex2;
uniform float mixValue;

void main()
{
    fragColor = mix(texture(tex2, vec2(1.0 - texCoord.x, texCoord.y)), vec4(vertexColor.zyx, 1.0), mixValue);
}